# Linked cloud project (migrations, `supabase link`, `db push`).
# WARNING: `supabase config push` uploads this file’s auth settings to that
# project. Do not run config push after adding local-only auth tweaks unless you
# intend to change hosted Auth.
project_id = "issiwryobnohfevlzyuu"

# Local `supabase start` only: sign-in works immediately after sign-up without
# opening Mailpit (emails still appear at http://127.0.0.1:54324 when enabled).
[auth.email]
enable_confirmations = false
