{
  "name": "@widados/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -w -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0 -c ../../.eslintrc.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "libphonenumber-js": "^1.12.42",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@widados/ts-config": "workspace:*",
    "typescript": "^5.6.2",
    "vitest": "^3.2.4"
  }
}
