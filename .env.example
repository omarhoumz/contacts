# Copy to apps/web/.env.local and apps/mobile/.env.local (gitignored). Never commit real keys.
#
# Easiest switches (from repo root):
#   pnpm env:supabase:local   # needs: cd apps/backend && pnpm db:start
#   pnpm env:supabase:cloud   # needs: apps/backend/supabase/.env.cloud (see .env.cloud.example there)

# --- Local Supabase (typical defaults) ---
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# --- Hosted project (staging / cloud) ---
# VITE_SUPABASE_URL=https://<project-ref>.supabase.co
# VITE_SUPABASE_ANON_KEY=<dashboard anon or publishable key>
# EXPO_PUBLIC_SUPABASE_URL=...
# EXPO_PUBLIC_SUPABASE_ANON_KEY=...
